allow:
  - net
  - env
envFile:
  - .env
imap: import_map.json

scripts:
  dev:
    desc: Runs the server
    cmd: server.ts
    watch: true
    unstable: true
  prod:
    cmd: server.ts
    unstable: true
    env:
      PORT: 80
